### Question 1: Simon's Algorithm

#### **(a) Give the starting state of Simon’s algorithm.给出西蒙算法的初始状态**  

The system starts in the all-zero state:  
系统从全零态开始：

\[
|\psi_0\rangle = |0\rangle^{\otimes 3} \otimes |0\rangle^{\otimes 3} = |000\rangle \otimes |000\rangle = |000000\rangle
\]

#### **(b) Give the state after the first Hadamard transforms on the first 3 qubits.**  
**(b) 在前三个比特上施加 Hadamard 变换后的状态。**

Applying Hadamard gates to the first three qubits yields:  
对前三个比特施加 Hadamard 变换后，状态变为：

\[
|\psi_1\rangle = H^{\otimes 3} |0\rangle \otimes |0\rangle
\]

Expanding the Hadamard transformation:  
展开 Hadamard 变换：

\[
|\psi_1\rangle = \frac{1}{\sqrt{8}} \sum_{x \in \{0,1\}^3} |x\rangle \otimes |000\rangle
\]

\[
= \frac{1}{2\sqrt{2}} (|000000\rangle + |001000\rangle + |010000\rangle + |011000\rangle + |100000\rangle + |101000\rangle + |110000\rangle + |111000\rangle)
\]

#### **(c) Give the state after applying the oracle.**  
**(c) 施加 Oracle 后的状态。**

Applying the oracle maps \(|x\rangle \to |x\rangle |f(x)\rangle\):  
Oracle 作用后，映射如下：\(|x\rangle \to |x\rangle |f(x)\rangle\)：

\[
|\psi_2\rangle = \frac{1}{\sqrt{8}} \sum_{x \in \{0,1\}^3} |x\rangle |f(x)\rangle
\]

Substituting the given function values:  
代入给定的函数值：

\[
|\psi_2\rangle = \frac{1}{2\sqrt{2}} ( |000000\rangle + |001001\rangle + |010010\rangle + |011011\rangle + |100011\rangle + |101010\rangle + |110001\rangle + |111000\rangle )
\]

#### **(d) Give the state after measuring the second register (suppose the measurement gave \(|001\rangle\)).**  
**(d) 测量第二个寄存器后的状态（假设测得 \(|001\rangle\)）。**

测量会导致整个系统坍缩为所有与 \( |001\rangle \) 相关的项：
The measurement causes the system to collapse into terms where the second register is \( |001\rangle \):

\[
|\psi_3\rangle = \frac{1}{\sqrt{2}} ( |001\rangle |001\rangle + |110\rangle |001\rangle )
\]

去掉重复的 \( |001\rangle \)：
After factoring out \( |001\rangle \):

\[
|\psi_3\rangle = \frac{1}{\sqrt{2}} ( |001\rangle + |110\rangle ) \otimes |001\rangle
\]

由于第二个寄存器已经被测量，最终状态只包含前三个比特：
Since the second register is now measured, we focus only on the first three qubits:

\[
|\psi_3\rangle = \frac{1}{\sqrt{2}} ( |001\rangle + |110\rangle )
\]

After measuring the second register and obtaining \(|001\rangle\), the first register collapses into a superposition of the corresponding \(x\) values:  
测量第二个寄存器并得到 \(|001\rangle\) 后，第一个寄存器坍缩为对应的 \(x\) 值的叠加态：

\[
|\psi_3\rangle = \frac{1}{\sqrt{2}} ( |001\rangle + |110\rangle )
\]

#### **(e) Using \( H^{\otimes n} |i\rangle = \frac{1}{\sqrt{2^n}} \sum_{j \in \{0,1\}^n} (-1)^{i \cdot j} |j\rangle \), give the state after the final Hadamards.**  
**(e) 使用 \( H^{\otimes n} |i\rangle = \frac{1}{\sqrt{2^n}} \sum_{j \in \{0,1\}^n} (-1)^{i \cdot j} |j\rangle \)，求出最后一次 Hadamard 变换后的状态。**

Applying the Hadamard transform to the first three qubits:  
在前三个比特上施加 Hadamard 变换：

\[
H^{\otimes 3} |i\rangle = \frac{1}{\sqrt{2^3}} \sum_{j \in \{0,1\}^3} (-1)^{i \cdot j} |j\rangle
\]

\[
|\psi_4\rangle = \frac{1}{2} ( |000\rangle + |011\rangle - |101\rangle - |111\rangle )
\]

#### **(f) Why does a measurement of the first 3 qubits of the final state give information about \( s \)?**  
**(f) 为什么最终状态的前三个比特的测量结果可以提供关于 \( s \) 的信息？**

By measuring the first three qubits, we are effectively measuring the superposition state after applying the Hadamard transform.  
通过测量前三个比特，我们实际上是在测量 Hadamard 变换后的叠加态。

The measurement result \( j \) must satisfy the equation:  
测量结果 \( j \) 必须满足方程：

\[
j \cdot s = 0
\]

The nonzero \( j \) values can be obtained by repeating the measurement process multiple times.  
通过多次重复测量，可以获得多个非零的 \( j \) 值。

Therefore, we can determine \( s \) by solving the linear equations.  
因此，我们可以通过解线性方程组来确定 \( s \)。

#### **(g) Suppose the first run of the algorithm gives \( j = 011 \) and a second run gives \( j = 101 \). Show that, assuming \( s \neq 000 \), these two runs already determine \( s \).**  
**(g) 假设算法的第一次运行给出 \( j = 011 \)，第二次运行给出 \( j = 101 \)。证明在 \( s \neq 000 \) 的情况下，这两次运行已经确定了 \( s \)。**

From the measurements, we obtain the equations:  
从测量结果，我们得到以下方程：

\[
j = 011: \quad 0 \cdot s_0 + 1 \cdot s_1 + 1 \cdot s_2 = 0
\]

\[
s_1 + s_2 = 0
\]

\[
j = 101: \quad 1 \cdot s_0 + 0 \cdot s_1 + 1 \cdot s_2 = 0
\]

\[
s_0 + s_2 = 0
\]

Solving the system of equations:  
求解这个方程组：

\[
\begin{cases}
s_1 + s_2 = 0 \\
s_0 + s_2 = 0
\end{cases}
\]

This implies \( s_0 = s_1 = s_2 \).  
这表明 \( s_0 = s_1 = s_2 \)。

Since \( s \neq 000 \), we conclude:  
由于 \( s \neq 000 \)，我们可以得出：

\[
s = 111
\]

Thus, the two runs of the algorithm are sufficient to determine \( s \).  
因此，这两次运行已经足以确定 \( s \)。

---

### Question 2： Grover
(Grover’s algorithm) 假设我们将 Grover 算法应用于一个 3 量子比特的寄存器，其中仅有状态 \( |010\rangle \) 被标记。  
(Grover’s algorithm) Suppose we apply Grover’s algorithm to a 3-qubit register, in which only the state \( |010\rangle \) is marked.  

在应用 Grover 迭代 0、1、2、3 次后，测量到标记状态 \( |010\rangle \) 的概率是多少？并绘制 Oracle \( V \) 的量子电路。 
What is the probability of measuring the marked state \( |010\rangle \) after applying the Grover iterate 0,1,2,3 times? And draw the quantum circuit of Oracle \( V \). 

Grover 算法的状态演化如下：  
The evolution of the state in Grover’s algorithm follows the equation:

\[
|\psi(k)\rangle_n = \cos((2k+1) \theta) |y\rangle + \sin((2k+1) \theta) |z\rangle
\]

其中 \( |y\rangle \) 是未标记的状态的均匀叠加态，\( |z\rangle \) 是被标记的状态 \( |010\rangle \)。  
where \( |y\rangle \) is the uniform superposition of unmarked states, and \( |z\rangle \) is the marked state \( |010\rangle \).  

#### **1. 当 \( k = 0 \)（未执行 Grover 迭代）**
**1. When \( k = 0 \) (No Grover iteration applied)**

初始状态为：
The initial state is:

\[
|\psi(0)\rangle_n = \cos \theta |y\rangle + \sin \theta |z\rangle
\]

所有 8 个状态的均匀叠加态：
The uniform superposition of all 8 states:

\[
| \psi \rangle = \frac{1}{\sqrt{8}} (|000\rangle + |001\rangle + |010\rangle + |011\rangle + |100\rangle + |101\rangle + |110\rangle + |111\rangle)
\]

因此：
Thus:

\[
\sin \theta = \frac{1}{\sqrt{2}\sqrt{4}} = \frac{1}{\sqrt{8}}, \quad \theta = \arcsin \frac{1}{\sqrt{8}}
\]

测得 \( |010\rangle \) 的概率：
The probability of measuring \( |010\rangle \):

\[
\text{Prob} \{|010\rangle\} = |\sin \theta|^2 = \frac{1}{8} = 0.125
\]

---

#### **2. 当 \( k = 1 \)（执行 1 次 Grover 迭代）**
**2. When \( k = 1 \) (After 1 Grover iteration)**

\[
|\psi(1)\rangle_n = \cos 3\theta |y\rangle + \sin 3\theta |z\rangle
\]

测得 \( |010\rangle \) 的概率：
The probability of measuring \( |010\rangle \):

\[
\text{Prob} \{|010\rangle\} = |\sin 3\theta|^2 = \frac{25}{32} \approx 0.781
\]

---

#### **3. 当 \( k = 2 \)（执行 2 次 Grover 迭代）**
**3. When \( k = 2 \) (After 2 Grover iterations)**

\[
|\psi(2)\rangle_n = \cos 5\theta |y\rangle + \sin 5\theta |z\rangle
\]

测得 \( |010\rangle \) 的概率：
The probability of measuring \( |010\rangle \):

\[
\text{Prob} \{|010\rangle\} = |\sin 5\theta|^2 = \frac{121}{128} \approx 0.945
\]

---

#### **4. 当 \( k = 3 \)（执行 3 次 Grover 迭代）**
**4. When \( k = 3 \) (After 3 Grover iterations)**

\[
|\psi(3)\rangle_n = \cos 7\theta |y\rangle + \sin 7\theta |z\rangle
\]

测得 \( |010\rangle \) 的概率：
The probability of measuring \( |010\rangle \):

\[
\text{Prob} \{|010\rangle\} = |\sin 7\theta|^2 = \frac{169}{512} \approx 0.33
\]


由此可见，在 \( k = 2 \) 时，测得正确解的概率最高，接近 1。  
From this, we see that when \( k = 2 \), the probability of measuring the correct solution is the highest, close to 1.

---